#include <cstdlib>
#define todo exit(-1)
/**
 * @brief 回文树
 * 
 */
struct eertree {

    /**
     * @brief 往回文树末尾添加一个字符
     * 
     * @param ch 添加的字符
     * @return int 出现了多少个新的本质不同回文子串，实际只会是0/1
     */
    int add(char ch) {
        todo;
    }
};